import React from 'react';

import Content from "@/components/Config/content";


function Config({ isPhone, theme }: any) {
  return <Content isPhone={isPhone} theme={theme} />;
};

export default Config;

import React from 'react';

import Content from "@/components/Environment";


function Environment({ isPhone, theme }: any) {
  return <Content isPhone={isPhone} theme={theme} />;
};

export default Environment;

import React from 'react';

import Content from "@/components/Environment/content";


const Env = ({ isPhone, theme }: any) => {
  return <Content isPhone={isPhone} theme={theme} />;
};

export default Env;
